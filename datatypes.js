"use strict" ;  //treat all js code as a newer  version

// alert(5+3)  we are using node.js not browser this alert is not supported like this 

let name = "vinay"  //string datatype
let age = 2  //number datatype (have range of 2 to the power of 53)
let isloggedin = true  // boolean datatype => true or false
let state = null                  //  standalone value (empty)
let isthere;                    //undefined (means variable is ok but no value is assigned)
// symbol   =>      unique

//object
console.log(typeof isthere)